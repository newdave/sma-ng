SMA_IMAGE_TAG=latest
SMA_PORT=8585
# Intel-only override; replace with your host render group ID if needed.
# RENDER_GID=992
# Intel-only override; needed when /dev/dri/card* is owned by the host video group.
# VIDEO_GID=44

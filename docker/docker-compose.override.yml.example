# docker-compose.override.yml.example
#
# The base compose file already publishes PostgreSQL on the Docker host.
# Prefer setting PGSQL_BIND_IP and PGSQL_PORT in docker/.env instead of using
# an override file. This example is intentionally empty so copying it has no
# effect beyond documenting that behavior.

#!/usr/bin/env bash
#MISE description="Pause remote SMA nodes (workers immediately stop picking up new jobs; HOST or HOSTS to scope)"
#MISE depends=["deploy:mise"]
set -euo pipefail

# shellcheck disable=SC1091
source "$(dirname "$0")/../../shared/deploy/lib.sh"
# shellcheck disable=SC1091
source "$(dirname "$0")/../../shared/deploy/cluster_action.sh"

cluster_action pause

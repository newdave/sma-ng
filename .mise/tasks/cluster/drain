#!/usr/bin/env bash
#MISE description="Drain remote SMA nodes (workers finish active jobs then go idle; HOST or HOSTS to scope)"
#MISE depends=["deploy:mise"]
set -euo pipefail

# shellcheck disable=SC1091
source "$(dirname "$0")/../../shared/deploy/lib.sh"
# shellcheck disable=SC1091
source "$(dirname "$0")/../../shared/deploy/cluster_action.sh"

cluster_action drain

#!/usr/bin/env bash
#MISE description="Start the daemon server (requires SQLite/PostgreSQL db_url in env or config)"
#MISE aliases=["media:daemon:start"]
set -euo pipefail

"$PY" daemon.py --host 0.0.0.0 --port 8585

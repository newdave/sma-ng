#!/usr/bin/env bash
#MISE description="Start the daemon server (requires SMA_DAEMON_DB_URL or db_url in config/daemon.json)"
#MISE aliases=["media:daemon:start"]
set -euo pipefail

"$PY" daemon.py --host 0.0.0.0 --port 8585
